package practisee.JavaGeeks;

import java.util.ArrayList;

public class Q17_Subarray {
    public static int helper(int []arr, int index, int product, int maxproduct, ArrayList<Integer> list){
//        if(index==arr.length-1) return maxproduct;
//        list.add(arr[index]);
//        product*=arr[index];
//        if(product==0) product=1;
//        maxproduct=Math.max(product,maxproduct);
//        helper(arr,index+1,product,maxproduct,list);
//        list.remove(arr[index]);
//        product/=arr[index];
//        maxproduct=Math.max(product,maxproduct);
//        helper(arr,index+1,product,maxproduct,list);
        return -1;
    }


    public static void main(String[] args) {
        int[] arr={1,-2,-3,0,-4,-5};



    }
}
